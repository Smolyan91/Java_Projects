package patterns.observer.weathrer;

/**
 * Интерфейс, реализуемый всеми визуальными элементами.
 * Каждый элемент должен реализовать метод display()
 */
public interface DisplayElement {
    void display();
}
